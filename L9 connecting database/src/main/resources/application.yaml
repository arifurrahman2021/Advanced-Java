spring:
  application:
    name: L8 List DynamicTable
  datasource:
    url: jdbc:mysql://localhost:3306/cdb
    username: root
    password: "#alvi#"
  jpa:
    hibernate:
      ddl-auto: update

logging:
  level:
    root: info
  file:
    name: product.log